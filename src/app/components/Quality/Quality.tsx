import * as React from 'react';
import styled from 'styled-components';

type QualityProps = {};

const Container = styled.div`
    display: flex;
`;

const Quality: React.FunctionComponent<QualityProps> = ({}) => {
    return <Container></Container>;
};

export default Quality;
