import * as React from 'react';
import styled from 'styled-components';

const AppWrapper = styled.div`
    padding: 50px 100px;
`;

const App = () => <AppWrapper>Test</AppWrapper>;

export default App;
